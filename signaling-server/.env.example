# Server configuration
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# Room management
# Inactive room expiry timeout in seconds (default: 300 seconds / 5 minutes)
ROOM_TTL_SECONDS=300
# Interval for background cleanup worker in seconds
CLEANUP_INTERVAL_SECONDS=30
